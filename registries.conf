# Podman does not assume a default registry for unqualified image names, unlike Docker.
# The dev containers CLI passes names verbatim as found in devcontainer.json or FROM instructions.
# Without this entry, starting a container with an unqualified name fails.
unqualified-search-registries = ["docker.io"]
